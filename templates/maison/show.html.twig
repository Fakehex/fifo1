{% extends 'base.html.twig' %}

{% block title %}Maison{% endblock %}

{% block body %}
    <h1>Maison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maison.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ maison.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ maison.Description }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ maison.points }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('maison_index') }}">back to list</a>

    <a href="{{ path('maison_edit', {'id': maison.id}) }}">edit</a>

    {{ include('maison/_delete_form.html.twig') }}
{% endblock %}
