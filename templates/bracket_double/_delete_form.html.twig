<form method="post" action="{{ path('bracket_double_delete', {'id': bracket_double.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bracket_double.id) }}">
    <button class="btn">Delete</button>
</form>
