{% extends 'base.html.twig' %}

{% block title %}Duel{% endblock %}

{% block body %}
    <h1>Duel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duel.id }}</td>
            </tr>
            <tr>
                <th>ScoreInscrit1</th>
                <td>{{ duel.scoreInscrit1 }}</td>
            </tr>
            <tr>
                <th>ScoreInscrit2</th>
                <td>{{ duel.scoreInscrit2 }}</td>
            </tr>
            <tr>
                <th>Tour</th>
                <td>{{ duel.tour }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('duel_index') }}">back to list</a>

    <a href="{{ path('duel_edit', {'id': duel.id}) }}">edit</a>

    {{ include('duel/_delete_form.html.twig') }}
{% endblock %}
