{% extends 'base.html.twig' %}

{% block title %}CategorieMatiere index{% endblock %}

{% block body %}
    <h1>CategorieMatiere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_matiere in categorie_matieres %}
            <tr>
                <td>{{ categorie_matiere.id }}</td>
                <td>{{ categorie_matiere.titre }}</td>
                <td>
                    <a href="{{ path('categorie_matiere_show', {'id': categorie_matiere.id}) }}">show</a>
                    <a href="{{ path('categorie_matiere_edit', {'id': categorie_matiere.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categorie_matiere_new') }}">Create new</a>
{% endblock %}
