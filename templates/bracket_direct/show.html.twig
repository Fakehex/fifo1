{% extends 'base.html.twig' %}

{% block title %}BracketDirect{% endblock %}

{% block body %}
    <h1>BracketDirect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bracket_direct.id }}</td>
            </tr>
            <tr>
                <th>TourActuel</th>
                <td>{{ bracket_direct.tourActuel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bracket_direct_index') }}">back to list</a>

    <a href="{{ path('bracket_direct_edit', {'id': bracket_direct.id}) }}">edit</a>

    {{ include('bracket_direct/_delete_form.html.twig') }}
{% endblock %}
